node_modules/
.sfdx/
.sf/
*.env
*.key
credentials.*

# dist/ is intentionally NOT ignored -- committed for plugin distribution

# User-generated test artifacts (created in user's project, not plugin dir)
sfagent-reports/
